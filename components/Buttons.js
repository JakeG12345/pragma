export const SaveButton = ({ onClick }) => {
  return (
    <button className='w-24 py-1 rounded-full text-xl font-semibold bg-sky-500 hover:bg-sky-600' onClick={onClick}>
        Save
    </button>
  )
}
